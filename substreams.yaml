specVersion: v0.1.0
package:
  name: substreams_subgraph_sql_protodefs
  version: v0.1.0
  url: https://github.com/streamingfast/substreams-subgraph-sql
  doc: |
    Protobuf definitions for Substreams Subgraph Sink modules.

    This package does not include any modules, it contains **only** Protobuf definitions for the sink config.

protobuf:
  files:
    - sf/substreams/sink/subgraph/v1/services.proto
  importPaths:
    - ./proto
